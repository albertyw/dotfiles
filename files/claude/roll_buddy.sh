#!/usr/bin/env bash
#
# Generates the claude code buddy

# curl -fsSL https://bun.sh/install | bash
npx any-buddy@latest \
    --species owl \
    --rarity legendary \
    --eye '✦' \
    --hat propeller \
    --shiny yes \
    --peak WISDOM \
    --dump CHAOS \
    --yes

# rm -r ~/.bun
